<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/admissionCounselorLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/appBackground"
    tools:context=".MainActivity">

    <!-- Yellow Section: Radio Group -->
    <RadioGroup
        android:id="@+id/rgStudentType"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="60dp"
        android:layout_marginEnd="32dp"
        android:background="@android:color/transparent"
        android:padding="16dp"
        app:layout_constraintBottom_toTopOf="@id/actvLocation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="8dp"
            android:text="Are you an:"
            android:textColor="@color/subTextColor"
            android:textSize="36sp" />

        <RadioButton
            android:id="@+id/rbInternational"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="International student"
            android:textColor="@color/subTextColor"
            android:textSize="28sp" />

        <RadioButton
            android:id="@+id/rbFromUS"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="From the US"
            android:textColor="@color/subTextColor"
            android:textSize="28sp" />

    </RadioGroup>

    <!-- Red Section: Auto-complete dropdown (filterable) -->
    <AutoCompleteTextView
        android:id="@+id/actvLocation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/border_background"
        android:padding="16dp"
        android:textSize="28sp"
        android:textColor="#050505"
        android:hint="Enter country"
        android:textColorHint="@color/promptTextColor"
        android:maxLines="1"
        android:inputType="text"
        android:completionThreshold="1"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/rgStudentType"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnSubmit" />

    <!-- Submit Button -->
    <Button
        android:id="@+id/btnSubmit"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="60dp"
        android:layout_marginEnd="60dp"
        android:layout_marginBottom="60dp"
        android:backgroundTint="@color/buttonColor"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"
        android:text="Submit"
        android:textColor="@color/buttonTextColor"
        android:textSize="22sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>